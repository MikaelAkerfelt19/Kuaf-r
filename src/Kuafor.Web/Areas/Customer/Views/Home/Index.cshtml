@model Kuafor.Web.Models.CustomerDashboardViewModel
@{
    ViewData["Title"] = "Pano";
    var name = User?.Identity?.IsAuthenticated == true && !string.IsNullOrEmpty(User?.Identity?.Name)
        ? User.Identity.Name
        : "Misafir"; // TODO: Backend sonrası gerçek ad/soyad bilgisi eklenecek.
}

<div class="container py-4">
    <div class="d-flex flex-column flex-lg-row gap-3 justify-content-between align-items-start align-items-lg-center mb-4">
        <div>
            <h2 class="mb-1">Merhaba, @name 👋</h2>
            <div class="text-muted">Randevularını buradan yönetebilirsin.</div>
        </div>
        <div class="w-100 w-lg-50">
            <input class="form-control form-control-lg" id="globalSearch" placeholder="Ara veya bir komut yaz (Ctrl + K)" autocomplete="off" />
        </div>
    </div>

    <partial name="_UpcomingAppointment" model="Model?.Upcoming" />
    <div class="my-4"><partial name="_QuickActions" /></div>
    <div class="mt-4"><partial name="_ServicesGrid" /></div>
</div>

<partial name="_CommandPalette" />

@section Scripts {
    <script src="~/js/command-palette.js"></script>
}